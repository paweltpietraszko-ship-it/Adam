# Portfolio notes

Before using this repository in applications, complete these GitHub-level metadata changes manually:

1. Rename the repository from `Adam` to `Triangulum`.
2. Set the repository description to: `Experimental multi-model LLM evaluation and synthesis PWA with adversarial checks and deterministic backend controls.`
3. Add topics such as: `llm-evaluation`, `ai-evaluation`, `multi-agent`, `adversarial-testing`, `human-in-the-loop`, `fastapi`, `pwa`.
4. Add the public Railway deployment URL as the repository website/homepage.
5. Confirm that the deployment is safe for public access and does not expose admin endpoints or credentials.

This file can be deleted after those metadata changes are completed.
