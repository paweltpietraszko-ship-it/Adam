# Project scope

Triangulum is a personal experimental application for comparing and evaluating outputs from multiple LLM providers. It is not a benchmark suite, a commercial model-training system or a claim of scientific superiority over single-model workflows.

The portfolio value of the project is the documented process of discovering model and orchestration failures, testing fixes and deciding which controls should remain probabilistic and which should be enforced deterministically in the backend.
